const SectionTitle = ({ text }) => {
  return (
    <div className="inline-block bg-black text-white px-4 py-1 mb-4 font-semibold text-sm">
      {text}
    </div>
  );
};

export default SectionTitle;
